<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To-Do List</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f2f2f2;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-image: linear-gradient(45deg, #f9fcde, #f2f2f2, #f9fcde); /* Linear gradient background */
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.3); /* Shadow */
            text-align: center;
        }

        .todo-list {
            list-style-type: none;
            padding: 0;
            margin-top: 20px;
        }

        .todo-list li {
            display: flex;
            align-items: center;
            margin: 10px 0;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        .tick-checkbox {
            margin-right: 10px;
        }

        form {
            display: flex;
            margin-top: 20px;
        }

        input[type="text"] {
            flex: 1;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        button {
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            padding: 10px 20px;
            cursor: pointer;
        }

        button:hover {
            background-color: #0056b3;
        }

        h1 {
            text-align: center;
            margin-top: 20px;
        }

       
        .tick-checkbox:checked ~ span {
            text-decoration: line-through;
            color: red;
        }
        
        h2 {
            font-family: 'Helvetica Neue', sans-serif;
            font-size: 36px;
            font-weight: bold;
            text-align: center;
            margin-top: 20px;
            animation: floatAnimation 5s infinite;
            transition: color 0.3s ease-in-out;
            cursor: pointer;
            position: relative;
            color: #6200ff;
        }

        @keyframes floatAnimation {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }
        h1:hover {
            color: #6200ff; 
        }

        /* Other styles for your container, todo-list, etc. */
        </style>
</head>
<body>
</script>
    <h2>DAILY CHECKER ☑️</h2>
    <h1><%=showDate %></h1>
    <div class="container">
        <ul class="todo-list">
            <!-- EJS loop to generate list items -->
            <% for (let i = 0; i < newListItem.length; i++) { %>
              <li>
                  <input type="checkbox" class="tick-checkbox">
                  <span><%= newListItem[i] %></span>
                  </span>
              </li>
          <% } %>
        </ul>
        <form action="/" method="post">
            <input type="text" name="newItem" autocomplete="off" placeholder="Add a task">
            <button type="submit">Add</button>
          
        </form>
    </div>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="script.js"></script>
</body>
</html>
